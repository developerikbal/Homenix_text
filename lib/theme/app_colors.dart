import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color primaryLight = Color(0xFF4CAF50);       // Light Green
  static const Color secondaryLight = Color(0xFFFFFFFF);     // White
  static const Color accentLight = Color(0xFFB2DFDB);        // Teal Accent
  static const Color textLight = Color(0xFF212121);          // Dark text for light background
  static const Color backgroundLight = Color(0xFFF5F5F5);    // Light background

  // Dark Theme Colors
  static const Color primaryDark = Color(0xFF2E7D32);        // Dark Green
  static const Color secondaryDark = Color(0xFF121212);      // Almost black
  static const Color accentDark = Color(0xFF80CBC4);         // Teal Accent
  static const Color textDark = Color(0xFFE0E0E0);           // Light text for dark background
  static const Color backgroundDark = Color(0xFF1E1E1E);     // Dark background

  // Common Colors
  static const Color error = Color(0xFFD32F2F);              // Red error color
  static const Color success = Color(0xFF388E3C);            // Green success
  static const Color warning = Color(0xFFFFA000);            // Amber warning
  static const Color info = Color(0xFF1976D2);               // Blue info
  static const Color disabled = Color(0xFFBDBDBD);           // Grey disabled
  static const Color divider = Color(0xFFBDBDBD);            // Divider color
}